# i3blocks config file
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
# Change $SCRIPT_DIR to the location of your scripts!
command=/home/kyeb/.scripts/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=none

[volume]
label=♪
interval=once
signal=10


[batterybar]
label=bat:
interval=5
markup=pango
min_width=bat: ■■■■■

[cpu_usage2]
markup=pango
interval=persist

#[memory]
#label=MEM
#interval=5

[disk]
label=home
interval=30

#[disk]
#label=shared
#DIR=/mnt/shared
#interval=30

[iface]
color=#00FF00
interval=10


# Date Time
[time]
command=date '+%b %d %I:%M %P'
interval=1

# Generic media player support
[mediaplayer]
instance=spotify
interval=5
signal=10

