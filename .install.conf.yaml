- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.zshrc:
    ~/.vim:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.config/alacritty/alacritty.yml:
        force: true
    ~/.config/sway:
    ~/.config/rofi:
    ~/.config/nnn:
    ~/.config/zathura:




- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [mkdir -p ~/code, Creating code directory]
  - [mkdir -p ~/.vim/.undo ~/.vim/.backup ~/.vim/.swp, Creating vim directories]

